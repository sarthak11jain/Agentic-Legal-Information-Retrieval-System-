# Maintained-code boundary

`src/legal_rag/` is the supported API and is covered by tests, linting, type
checking, packaging checks, and the data-free CLI demo.

`archive/competition-2026/` is a preserved research snapshot. It is compiled
in CI to catch syntax regressions, but it is not a stable API and is not covered
by the maintained-package quality gate.
